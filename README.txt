To set up a rails app on Mac OS X or Linux (Ubuntu was tested), you can just do:

cd YOUR_RAILS_APP
git submodule add git://github.com/johnae/rails_supervision.git runit
cd YOUR_RAILS_APP/runit
run install